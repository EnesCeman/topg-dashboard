{
    "buildCommand": "npm i && cd client && npm run build",
    "outputDirectory": "/client/build",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/server.js"
        }
    ]
}